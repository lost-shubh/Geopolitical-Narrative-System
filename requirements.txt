# ============================================================================
# Geopolitical Narrative Intelligence System - Dependencies
# ============================================================================
# Python 3.8+ required
# Install: pip install -r requirements.txt
# ============================================================================

# ----------------------------------------------------------------------------
# CORE DEPENDENCIES
# ----------------------------------------------------------------------------
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0

# ----------------------------------------------------------------------------
# NLP & TEXT PROCESSING
# ----------------------------------------------------------------------------

# Transformers & Deep Learning
transformers>=4.35.0
torch>=2.0.0
sentence-transformers>=2.2.2
tokenizers>=0.15.0

# spaCy (Named Entity Recognition, Dependency Parsing)
spacy>=3.7.0
# Install language models separately:
# python -m spacy download en_core_web_sm
# python -m spacy download en_core_web_lg

# Traditional NLP
nltk>=3.8.1
# Download required: 
# python -c "import nltk; nltk.download('punkt'); nltk.download('stopwords'); nltk.download('vader_lexicon')"

# Language Detection
langdetect>=1.0.9
#fasttext>=0.9.2

# Advanced NLP
flair>=0.13.0              # For advanced NER
stanza>=1.7.0              # Stanford NLP

# ----------------------------------------------------------------------------
# TOPIC MODELING & CLUSTERING
# ----------------------------------------------------------------------------
bertopic>=0.16.0           # BERT-based topic modeling
gensim>=4.3.0              # LDA, Word2Vec
scikit-learn>=1.3.0        # Clustering, classification
umap-learn>=0.5.5          # Dimensionality reduction
hdbscan>=0.8.33            # Density-based clustering

# ----------------------------------------------------------------------------
# SENTIMENT & EMOTION ANALYSIS
# ----------------------------------------------------------------------------
vaderSentiment>=3.3.2      # Rule-based sentiment
textblob>=0.17.1           # Simple sentiment analysis

# Note: Deep learning emotion models loaded via transformers:
# - j-hartmann/emotion-english-distilroberta-base
# - SamLowe/roberta-base-go_emotions

# ----------------------------------------------------------------------------
# WEB SCRAPING & DATA COLLECTION
# ----------------------------------------------------------------------------

# Web Scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0
selenium>=4.15.0
scrapy>=2.11.0
playwright>=1.40.0         # Modern web scraping

# HTML/Text Extraction
trafilatura>=1.6.0         # Clean web content extraction
newspaper3k>=0.2.8         # Article extraction
readability-lxml>=0.8.1    # Main content extraction
html2text>=2024.2.26       # HTML to markdown

# HTTP Clients
httpx>=0.25.0              # Async HTTP client
aiohttp>=3.9.0             # Async requests

# ----------------------------------------------------------------------------
# SOCIAL MEDIA APIs
# ----------------------------------------------------------------------------

# Twitter/X
tweepy>=4.14.0             # Twitter API v2

# Reddit
praw>=7.7.1                # Reddit API wrapper

# YouTube
google-api-python-client>=2.108.0

# Facebook (unofficial)
facebook-scraper>=0.2.59

# Telegram
telethon>=1.33.0

# ----------------------------------------------------------------------------
# NEWS & CONTENT APIs
# ----------------------------------------------------------------------------
newsapi-python>=0.2.7      # NewsAPI client
feedparser>=6.0.10         # RSS/Atom feed parser
#python-gnews>=0.3.7        # Google News scraper

# ----------------------------------------------------------------------------
# FACT-CHECKING & VERIFICATION
# ----------------------------------------------------------------------------

# Claim Detection
#claimbuster>=0.1.0         # Claim detection (if available)

# Google Fact Check Tools
#google-cloud-factcheck>=0.1.0  # Google Fact Check API (if available)

# Web Search
googlesearch-python>=1.2.3
duckduckgo-search>=3.9.0

# PDF Processing (for research papers)
PyPDF2>=3.0.0
pdfplumber>=0.10.0
pymupdf>=1.23.0            # Fast PDF processing

# ----------------------------------------------------------------------------
# ACADEMIC PAPER SEARCH
# ----------------------------------------------------------------------------
semanticscholar>=0.8.0     # Semantic Scholar API
pymed>=0.8.9               # PubMed access
arxiv>=2.1.0               # arXiv API

# ----------------------------------------------------------------------------
# DATABASE & STORAGE
# ----------------------------------------------------------------------------
sqlalchemy>=2.0.0          # SQL toolkit
psycopg2-binary>=2.9.9     # PostgreSQL adapter
pymongo>=4.6.0             # MongoDB
redis>=5.0.0               # Redis cache

# Vector Databases (for semantic search)
chromadb>=0.4.20           # Vector database
faiss-cpu>=1.7.4           # Facebook AI Similarity Search
# Use faiss-gpu if CUDA available

# ----------------------------------------------------------------------------
# DATA VALIDATION & PROCESSING
# ----------------------------------------------------------------------------
pydantic>=2.5.0            # Data validation
jsonschema>=4.20.0         # JSON schema validation
python-dateutil>=2.8.2     # Date parsing
pytz>=2024.1               # Timezone handling

# ----------------------------------------------------------------------------
# LOGGING & MONITORING
# ----------------------------------------------------------------------------
loguru>=0.7.2              # Better logging
python-json-logger>=2.0.7  # JSON logging
tqdm>=4.66.0               # Progress bars

# ----------------------------------------------------------------------------
# VISUALIZATION & ANALYSIS
# ----------------------------------------------------------------------------
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
wordcloud>=1.9.3           # Word clouds
networkx>=3.2.0            # Network graphs
pyvis>=0.3.2               # Interactive network viz

# ----------------------------------------------------------------------------
# WORKFLOW & PIPELINE
# ----------------------------------------------------------------------------
prefect>=2.14.0            # Workflow orchestration (alternative to Nextflow)
apache-airflow>=2.8.0      # Production workflow (optional, heavy dependency)
celery>=5.3.0              # Distributed task queue
schedule>=1.2.0            # Simple job scheduling

# ----------------------------------------------------------------------------
# API & WEB FRAMEWORK (for future dashboard)
# ----------------------------------------------------------------------------
fastapi>=0.108.0           # Modern API framework
uvicorn>=0.25.0            # ASGI server
streamlit>=1.29.0          # Quick dashboards
gradio>=4.12.0             # ML model interfaces

# ----------------------------------------------------------------------------
# TESTING
# ----------------------------------------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
hypothesis>=6.92.0         # Property-based testing
faker>=21.0.0              # Fake data generation

# ----------------------------------------------------------------------------
# CODE QUALITY
# ----------------------------------------------------------------------------
black>=23.12.0             # Code formatter
flake8>=7.0.0              # Linting
mypy>=1.8.0                # Type checking
isort>=5.13.0              # Import sorting
pylint>=3.0.0              # Code analysis

# ----------------------------------------------------------------------------
# SECURITY
# ----------------------------------------------------------------------------
cryptography>=41.0.0       # Encryption
python-jose>=3.3.0         # JWT tokens
passlib>=1.7.4             # Password hashing

# ----------------------------------------------------------------------------
# CLOUD & DEPLOYMENT (Optional)
# ----------------------------------------------------------------------------
# boto3>=1.34.0            # AWS SDK
# google-cloud-storage>=2.14.0  # Google Cloud Storage
# azure-storage-blob>=12.19.0   # Azure Blob Storage

# ----------------------------------------------------------------------------
# UTILITIES
# ----------------------------------------------------------------------------
click>=8.1.7               # CLI framework
rich>=13.7.0               # Rich terminal output
tenacity>=8.2.3            # Retry logic
python-magic>=0.4.27       # File type detection

# ----------------------------------------------------------------------------
# RATE LIMITING & CACHING
# ----------------------------------------------------------------------------
ratelimit>=2.2.1
cachetools>=5.3.0

# ----------------------------------------------------------------------------
# DOCUMENTATION
# ----------------------------------------------------------------------------
# sphinx>=7.2.0            # Documentation generator
# sphinx-rtd-theme>=2.0.0  # Read the Docs theme

# ============================================================================
# NOTES
# ============================================================================
#
# 1. INSTALLATION ORDER:
#    - Install PyTorch first if using GPU:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#    - Then install requirements:
#      pip install -r requirements.txt
#
# 2. OPTIONAL HEAVY DEPENDENCIES:
#    - Apache Airflow (comment out if not needed)
#    - Selenium/Playwright (only if scraping dynamic sites)
#    - Cloud SDKs (only if deploying to cloud)
#
# 3. POST-INSTALLATION STEPS:
#    - Download spaCy models: python -m spacy download en_core_web_sm
#    - Download NLTK data: python -c "import nltk; nltk.download('all')"
#    - Configure environment variables in .env file
#
# 4. SYSTEM DEPENDENCIES (install before pip):
#    - For psycopg2: sudo apt-get install libpq-dev (Ubuntu/Debian)
#    - For lxml: sudo apt-get install libxml2-dev libxslt1-dev
#
# 5. MINIMAL INSTALLATION (for testing only):
#    Create requirements-minimal.txt with just:
#    - numpy, pandas, requests, transformers, torch, spacy, beautifulsoup4
#
# 6. VERSION PINNING:
#    - Current versions are minimum required
#    - Use >= for flexibility, == for reproducibility
#    - Generate lock file: pip freeze > requirements-lock.txt
#
# ============================================================================